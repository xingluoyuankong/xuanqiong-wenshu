# AGENTS.md — 玄穹文枢 项目级代理规则

## 输出硬规则（最高优先级，覆盖任何叙述习惯）

调用工具之前，只有两个合法选择：

1. 写一句完整、有信息量的中文句子（说明发现了什么、改了什么、为什么）。
2. 什么都不写，直接调用工具。

禁止输出的内容：

- `...`、`…`、`。。。` 或任何只由标点、点号、空白组成的消息。
- 「让我看一下」「现在我来」「接下来我先」这类只声明意图、不含信息的垫话。
- 任何在工具调用前用于占位的文本。

根因备忘：这类「...」不是工具或终端的输出，而是代理在工具调用前
习惯性垫一句话、又没有实质内容时退化出来的空文本。修复方式是取消
「工具调用前必须先说话」这个习惯，而不是把垫话换成别的符号。

## 工程规则

1. 保留全部现有未提交改动。禁止 `reset --hard`、批量删除、覆盖旧成果、不可逆迁移。
2. 以当前文件、实测结果和真实运行行为为准，不采信旧「生产就绪」报告。
3. 不允许通过降低标准、删测试、跳过测试、只改报告来制造通过。
4. 新增修复必须配回归测试，并做反向验证：故意破坏实现后测试应当失败。
5. 后端验证：`cd backend; .\.venv\Scripts\python.exe -m pytest -q`。
6. 前端验证：`npm run type-check`、`npm run test:run`、`npm run build-only`。
