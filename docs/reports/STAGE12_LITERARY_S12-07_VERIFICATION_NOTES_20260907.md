# S12-07 复核补充与证据边界

记录时间：2026-09-07T07:16:37.133016+00:00。

本补充应与 `D:/小说写作/xuanqiong-wenshu/docs/reports/STAGE12_LITERARY_S12-07_20260907.md` 一起阅读，原有报告保留。

## 已核验与证明范围

- 标签 CSV/manifest 与本轮首次重算前指纹一致：True。
- 内存变异涉及的审计器、测试及生产 pipeline 文件与变异前指纹一致：True。
- 846 文件指纹为事后快照，仅用于后续对账，不是本轮全范围前后无漂移证明。
- 首轮 Python/pytest 没有显式禁用 bytecode/cache，也没有将临时夹具目录重定向到限定写集。故本轮严格写集遵守情况存在审计缺口，未证明所有附带写入均位于允许路径；保留现有痕迹，不清理缓存或临时文件掩盖这一点。业务文件与标签内容的有限哈希证明，不外推为全工作区零写入证明。
- 本轮没有运行后端全量或前端三门禁，也没有发起 Provider 生成、数据库连接或标签填写。此前工程门禁不是本轮结果。

## 口径澄清

- 复制脚本保持相同目录层级，仓库根解析有效；本轮中途关于目录假设有误的解说属于误判，不撤销首次重算结果。
- completion JSON 的 audit_date=2026-08-23 是代码中的历史固定元数据；本次实际执行日为 2026-09-07。
- completion 审计器读取 gap register/matrix 并核验 reviewer 来源链；七项文学收益是否成立还需逐项证据重算，不能仅凭 CLI exit0 判断。
- T16 的 comparable_pair_count=2 是既有审计器判定的结构可比配对，不等同于经版本身份、独立冻结 evaluator 和内容摘要绑定验证的生产优化前后证据。
- 37 是四份 CSV 的行数。基础包19项、T18包6项，后者在 labels 与两份 reviewer 模板中重复；不是37个独立已审样本。人工字段及身份填写均为0。
- T18 不是从未触发：既有记录有真实豁免触发但最终质量门拒绝；另有5条成功且未触发的对照。独立人工质量真值仍缺失。
- 旧文学报告已包含 T16/模板数量的上述限定，主报告“更正”一节应理解为重申和补充，未发现其最新段落与本轮核心数字冲突。

## 复跑时限定缓存与临时输出

在工作区用 PowerShell 设置 `PYTHONDONTWRITEBYTECODE=1`、`PYTHONIOENCODING=utf-8`，并将 `PYTEST_ADDOPTS` 设为 `-p no:cacheprovider`。
定向 pytest 另指定一个新建的 `D:/小说写作/xuanqiong-wenshu/logs/stage12-literary-S12-07/pytest-<实际运行标识>` 作为 basetemp；不要复用已有临时目录。
现成审计器使用绝对 output 路径和新的运行目录，保留全部旧产物。只复跑现成审计/内存变异，不运行生成器、数据库任务或人工标签合并写入。

最终结论不变：本轮证据复核完成；S12-07 真实文学质量达标未完成；completion_eligible=false，七项 hard gap 保留。
