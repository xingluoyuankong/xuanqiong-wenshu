# 生成/重写质量收口验证报告（2026-05-21）

## 本轮改动重点

- 继续把生成/重写首稿质量放在第一优先级：强化正文目标字数契约、场景预算、事件密度、对话改局、章末压力和首稿质量门。
- 优化/重写阶段默认局部锚点补丁，不再把整章重写当自动兜底；只有显式允许时才进入整章级 fallback。
- 把一致性修复、扩写、自检精修和优化都接入连续性关键词守卫，防止丢失章节任务、角色状态、关键物品地点、伏笔和因果压力。
- 写后账本继续闭环：角色状态、动态角色入池、时间线、因果链、伏笔/线索、知识图谱同步均写入 runtime metadata 和详细日志。
- 前端生成状态日志可以直接看到草稿片段、质量门、局部补丁建议、账本更新和诊断信息，不再只显示程序运行日志。

## 真实运行证据

项目本体：`D:\小说写作\xuanqiong-wenshu`

服务地址：

- 后端：`http://127.0.0.1:8013/api/health`
- 前端：`http://127.0.0.1:5174`
- CPA：仅作为 `/v1` provider 使用，不作为应用本体

实跑项目：

- 项目 ID：`35cbd8ec-6fdb-47d1-bf6d-437970143d4e`
- 项目名：`Codex实跑验证-20260520`

结果：

- 第 5 章：质量门正确阻断，原因包含 `critical`/`major` 一致性残留和章节推进不足；该失败被保留为“不能静默成功落库”的验证样例。
- 第 6 章：真实生成并定稿成功，候选版本 `332`，最终章节字数约 `6062`。
- 第 6 章写后闭环：角色状态 `6`、时间线事件 `11`、因果链 `6`、伏笔强化 `4`、自动收集伏笔 `2`、线索创建 `2`、线索更新 `5`、知识图谱边 `146`。
- 运行日志页能显示第 6 章“定稿闭环完成”“记忆层更新完成”“伏笔闭环完成”等生成状态。
- 第 7 章长章压力样例：目标 `8500`、最低 `7600`，真实进入 `prepare_context / longform_context / generate_variants`，但 CPA Provider 在正文候选生成阶段超过 20 分钟未返回。该实跑暴露了“长章单次 Provider 调用等待期间心跳不足、软降级不足”的问题；已转成代码修复：长章正文调用增加 Provider 等待心跳、取消检测、软超时中止和降 `max_tokens` 重试，不把正文机械切碎。

## 多视角评判转代码

- 作者视角：第 5 章失败样例说明旧链路可能把结构冲突内容推进到确认阶段；已强化质量门和失败日志，阻止静默成功。
- 编辑视角：第 6 章信息密度高但中段仍可能堆证据；首稿质量门已补充事件密度、对话改局和章末压力诊断，后续候选会给更明确的 patch suggestions。
- 读者视角：运行日志从“后台程序日志”转为“生成状态日志”，能看到正在写什么、为什么卡住、哪里需要补丁。
- 连续性审校视角：局部修补加入前后锚点和连续性关键词守卫，避免优化阶段损伤章节任务、伏笔和角色状态。
- 系统稳定性视角：活跃调用继续使用 `generation_call_service.py` 的可靠调用工具箱，并通过全量后端/前端测试验证。
- 长章压力视角：8500 字整章生成对 Provider 响应时间非常敏感；本轮没有把它改成碎片化正文，而是在可靠调用工具箱中补了软超时和心跳，让失败变成可见、可取消、可降级重试。

## 验证结果

- `python -m pytest backend -q`：`235 passed`
- `python -m compileall backend/app`：通过
- `cd frontend; npm run test:run`：`116 passed`
- `cd frontend; npm run build`：通过
- 浏览器验证：
  - 页面：`/workspace`、`/novel/35cbd8ec-6fdb-47d1-bf6d-437970143d4e`、`/admin?tab=runtime-logs`
  - 视口：`1440x950`、`390x844`
  - 结果：无 console error、无 page error、无 failed request、无横向溢出

## 保留风险

- 第 5 章仍保持失败状态，这是质量门阻断真实坏候选的结果；需要用户在 UI 中重试该章，或直接从第 7 章继续推进。
- 第 7 章压力样例为主动取消后的失败状态，用于验证 Provider 长等待暴露和取消路径；后端 `8013` 已重启，新软超时逻辑会在下一次真实长章生成中生效。
- 数据库中存在历史项目名乱码样例，本轮没有改旧数据迁移，只验证新日志和当前链路行为。
