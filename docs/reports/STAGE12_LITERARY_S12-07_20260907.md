# 阶段十二 S12-07 文学质量证据复核

> 复核日期：2026-09-07（Asia/Shanghai）  
> 复核范围：只读重算当前质量审计 JSON 与既有证据；新增写集仅为 `logs/stage12-literary-S12-07/` 与本报告。  
> 约束核验：未生成 Provider 调用，未填写人工标签，未修改 backend 业务源、前端、正文、数据库或既有人工标签 CSV。

## 1. 最终结论

- **文学质量复核：已完成本轮只读证据复核。**
- **completion_eligible：`false`。**
- **goal_status：`active`；hard gap：7 项，全部保留。**
- 本轮产物是证据重算与缺口确认，不是文学质量完成证明，也不改变旧报告。

## 2. 七项 gap 实测结果

| 项目 | 当前 status | 权威证据 | 缺口 | 下一动作 |
|---|---|---|---|---|
| E01.2 Prompt gain | 未闭合：controlled_ab_observed_not_strict_t16 | `logs/stage12-literary-S12-07/observations.json`；10 条配对，均值分差 +144.4，中位数 -6.0，上升/下降/持平 4/5/1，LOO 范围 -15.11～282.67。 | 只有受控 A/B 观测；均值受离群项影响且中位数为负，未证明生产文学收益、人工偏好或严格 T16 before/after。 | 固定同 Provider/model/scorer/mission contract 的生产版本前后批次，保留全分布、失败、内容摘要与版本身份。 |
| E11/T22 repair gain | `insufficient`；gain_claimed=false | `logs/stage12-literary-S12-07/e11-t22-audit.json`；2 条 repair entry、2 次 attempted、diagnostics 0、strict-subset improvement 0、passed-after 0、unchanged 2。 | 没有完整真实质量门进入、修复前后 diagnostics、严格 issue-code 子集改善和通过后证据。 | 同一真实失败文本形成 before/after diagnostics，证明实际 repair 改善且质量门通过。 |
| T06 degrade rate | `insufficient`；三项 rate=null | `logs/stage12-literary-S12-07/t06-audit.json`；可用 cohort 2 批/20 次调用，retry events 0；另 1 批 Provider 阻断被排除；单 Provider/model contract=false，minimum_sample=false。 | 两批契约不同，不能合并为统一 cohort；0 次 retry 不等于退化率为 0。 | 同一 contract 连续批次达到既定最小样本，完整记录 retry_events，Provider 最终阻断单独列示。 |
| T16 before/after | 未闭合：严格收益证据不存在 | `logs/stage12-literary-S12-07/t16-audit.json`；扫描49个summary，23个完整指纹，12个候选配对，2个可比配对。2个可比配对只是同条件重复批次。 | 没有同 scorer/契约、生产版本身份和有效内容摘要绑定的优化前后配对；不能把重复批次或 E01.2 A/B 当 T16 收益。 | 冻结 evaluator、生成输入、生产版本和内容摘要，采集真实 before/after 并逐任务严格比较。 |
| T18 exemption quality truth | 未闭合：工程触发/可观测性不等于质量真值 | 既有 `backend/output/t18-real-exemption-triggered-longform-audit-20260823.json` 显示触发但 quality gate rejected；`t18-real-asgi-exemption-observability-audit-20260823.json` 为可观测性证据；当前 completion 仍将其列为 blocker。 | 没有独立人工审阅证明豁免样本的文学质量、误杀率或成功质量。 | 保留真实豁免触发记录，配独立人工审阅与成功/拒绝分层统计；AI字段只能作为辅助。 |
| T26 dialogue calibration | 夹具合同通过，不是语料校准 | `logs/stage12-literary-S12-07/observations.json`；10/10 fixture passed，`human_labels_available=false`，`corpus_precision_recall_claim=false`。29 条回归全通过。 | 没有真实语料分层抽样与独立人工标签，不能计算 production precision/recall。 | 从真实章节 manifest 分层抽样，完成两位独立审阅者标签、身份绑定和仲裁，再计算 precision/recall。 |
| human labels | 未闭合：0 行人工真值 | `logs/stage12-literary-S12-07/label-inventory.json`；4 个 CSV、37 行（含模板重复），any_filled=0、complete=0、reviewer_identity=0、identity_errors=0。 | CSV 是空审阅模板/检测字段，不能当人工真值；AI预标注和自动检测字段不计入 human labels。 | 由真实 reviewer 填写标签、身份、备注并按 manifest/content hash 对齐；有分歧时使用有来源的人工仲裁。 |

## 3. completion_eligible 核对

当前审计输出：
```json
{
  "completion_eligible": false,
  "goal_status": "active",
  "blockers": [
    "E-01.2_prompt_gain",
    "E-11_T-22_real_repair_gain",
    "T-06_degrade_rate",
    "T-16_real_before_after",
    "T-18_exemption_quality_truth",
    "T-26_dialogue_marker_calibration",
    "human_quality_labels"
  ]
}
```

判定：七项 blocker 均仍存在，因此本轮不具备完成资格。审计 CLI exit 0 仅表示审计运行完成，不代表文学质量完成；E11/T22、T06 的 exit 2 表示证据不足状态。

## 4. 只读审计与反向验证

- T26/E01.2 审计：exit 0；状态分别为 `fixture_contract_passed` 与 `controlled_ab_observed_not_strict_t16`。
- 定向回归：29 passed；变异验证：7/7 detected，`source_hashes_unchanged=true`、`all_detected=true`。
- 变异覆盖：摘要聚合、重复 mission、错误 variant、非 Prompt 生成参数、mission 指纹、内容路径边界、强制 T26 全通过。
- 标签盘点：未写入标签 CSV/manifest；`provider_calls=false`。
- 保护文件指纹已保存于 `logs/stage12-literary-S12-07/protected-integrity-after.json`，用于本轮只读边界留痕。

## 5. 本轮新增输出

| 路径 | 用途 |
|---|---|
| `logs/stage12-literary-S12-07/observations.json` | T26/E01.2 只读机器结果 |
| `logs/stage12-literary-S12-07/completion-audit.json` | completion 资格与 blocker 重算 |
| `logs/stage12-literary-S12-07/e11-t22-audit.json` | E11/T22 repair gain 重算 |
| `logs/stage12-literary-S12-07/t06-audit.json` | T06 retry/degrade cohort 重算 |
| `logs/stage12-literary-S12-07/t16-audit.json` | T16 全量可比性扫描 |
| `logs/stage12-literary-S12-07/label-inventory.json` | human labels 只读盘点 |
| `logs/stage12-literary-S12-07/mutation-results.json` | 内存变异反向验证 |
| `logs/stage12-literary-S12-07/evidence-manifest.json` | 新增证据 SHA256 清单 |
| `docs/reports/STAGE12_LITERARY_S12-07_20260907.md` | 本轮最终汇总报告 |

## 6. 旧报告口径更正

旧报告 `docs/reports/LITERARY_QUALITY_GAP_EVIDENCE_20260906.md` 保留不改。本报告仅补充并更正本轮口径：
- “T16 可比配对”只说明结构上可比；2 个可比配对是重复批次，不是优化前后收益。
- T26 的 10/10 仅为 fixture contract pass，不是生产语料 precision/recall。
- 4 个 CSV 的 37 行不能作为 37 个独立人工样本；模板与检测字段均不构成人工真值。
- `completion_eligible=false` 仍是当前权威结论。

## 7. 后续动作

1. 先补人工标签与 reviewer 身份/仲裁链，再重新计算 T26 与 human labels。
2. 在同一 contract 下补 T06 连续批次，并保持 Provider 阻断独立统计。
3. 采集严格 T16 生产 before/after；禁止用重复批次或 Prompt A/B 替代。
4. 形成真实 E11/T22 repair 前后 diagnostics 与严格子集改善证据。
5. 为 T18 exemption 样本补独立人工质量审阅。
