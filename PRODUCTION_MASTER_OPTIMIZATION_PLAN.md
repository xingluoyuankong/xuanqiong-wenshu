# 玄穹文枢：全功能生产级优化总计划

> 版本：2026-08-14
> 
> 用途：作为当前持续任务的唯一执行计划、依赖图、验收协议和发布门禁。本文档只定义计划和证据标准；“已实现”必须以代码、测试、真实入口和数据库证据同时确认。

## 0. 执行原则

1. 保留当前全部未提交改动；不执行硬重置、批量删除、覆盖旧成果或不可逆迁移。
2. 先修复可复现 P0，再做结构拆分和视觉优化；任何“优化”都必须有回归测试。
3. 所有外部任务状态以持久化 `TaskRuntime` 为真相源；内存对象只能保存本进程 worker 句柄或有明确失效策略的缓存。
4. 前端配置、API schema、后端 schema、数据库模型和任务 payload 必须形成同一份可追溯契约。
5. 每个功能都执行：现状盘点 → 逻辑链审计 → 复现缺陷 → 最小修复 → 正向测试 → 反向破坏测试 → 集成测试 → 真实入口 → 评分 → 再优化。
6. 不用单元测试、报告文字、模拟 Provider 或一次短章成功替代真实入口验收。
7. 并行只用于互不重叠的审查和实现；涉及同一数据模型、状态机、编排入口时由主线程统一合并。

## 1. 当前基线和状态定义

### 1.1 当前基线

- DeepSeek 默认模型已经切换为 `deepseek-v4-flash-free`。
- 已有 TaskRuntime、持久化事件、Alembic 迁移、SSE 游标、短章质量门、长篇分段 checkpoint、前端正文/日志分流基础。
- 已完成一次真实 DeepSeek 短章烟测：正文落库、正文增量、终态事件和 Last-Event-ID 回放均有证据。
- 当前不能宣称最终生产就绪：真实 1—10 章、真实 2 万字分段恢复、双项目并发、全部后台任务真实恢复、DOCX 往返、唯一任务中心和完整浏览器流程仍需验收。

### 1.2 状态标签

- `PENDING`：尚未实现或尚未形成可运行路径。
- `IMPLEMENTED_UNVERIFIED`：代码存在，但缺少正式真实入口证据。
- `TESTED`：有自动化测试，但尚未完成真实 Provider/浏览器验收。
- `REAL_VERIFIED`：真实隔离环境完成，证据包含 task、event、DB、输出和耗时。
- `RELEASED`：连续两轮全量门禁、真实核心链路和评分均达标。

## 2. 总体逻辑树

```text
玄穹文枢生产级系统
├─ A 基础与安全
│  ├─ A1 登录/鉴权/会话/用户隔离
│  ├─ A2 项目生命周期与权限
│  ├─ A3 模型、提示词、参数、Token预算配置
│  └─ A4 数据库迁移、租约、索引、审计
├─ B 统一任务中心
│  ├─ B1 TaskRuntime状态机/幂等/租约/心跳
│  ├─ B2 任务创建/去重/取消/重试/stale恢复
│  ├─ B3 持久化事件与运行日志
│  ├─ B4 SSE鉴权/游标/回放/心跳/终态关闭
│  └─ B5 跨项目、跨用户并发隔离
├─ C 创作前工程
│  ├─ C1 灵感与创作蓝图
│  ├─ C2 研究资料/文献/内容收集
│  ├─ C3 文风提取与风格库
│  ├─ C4 详细长篇总纲/卷纲/故事弧
│  └─ C5 章节大纲/场景账本/章节依赖
├─ D 正文生成工程
│  ├─ D1 短章单次正文
│  ├─ D2 中长章候选/评审/补足
│  ├─ D3 2万字以上分段/checkpoint/续跑
│  ├─ D4 续写/改写/局部修订/补丁
│  ├─ D5 元文本、格式、角色和安全守卫
│  └─ D6 质量门/一致性门/候选定稿
├─ E 长篇连续性账本
│  ├─ E1 项目记忆/章节摘要/快照
│  ├─ E2 人物状态/关系/势力
│  ├─ E3 伏笔/线索/回收状态
│  ├─ E4 时间线/因果链/事件图
│  └─ E5 情绪曲线/节奏/章间承接
├─ F 业务工作台
│  ├─ F1 项目/小说/章节/版本/阅读器
│  ├─ F2 角色/势力/关系编辑
│  ├─ F3 线索/伏笔/时间线/知识图谱
│  ├─ F4 评审/版本差异/补丁/定稿
│  ├─ F5 导入/导出/TXT/DOCX
│  └─ F6 管理台/日志/统计/限额
└─ G 前端体验与发布
   ├─ G1 左侧导航和信息架构
   ├─ G2 右上角进度卡与多任务切换
   ├─ G3 正文流和日志流视觉分区
   ├─ G4 紧凑卡片/响应式/键盘/可访问性
   ├─ G5 浏览器端完整流程
   └─ G6 门禁、观测、审计、发布
```

## 3. 依赖树和执行顺序

```text
A1/A2/A4 安全与数据基础
        ↓
B1 TaskRuntime状态机
        ↓
B2 幂等/租约/恢复 ───────┐
        ↓                 │
B3 事件/日志/B4 SSE       │
        ↓                 │
B5 并发隔离               │
        ↓                 │
C1-C5 创作前任务 ─────────┤
        ↓                 │
D1-D6 生成与质量门 ────────┤
        ↓                 │
E1-E5 定稿账本原子提交 ────┤
        ↓                 │
F1-F6 业务数据和导入导出 ──┤
        ↓                 │
G1-G5 前端真实工作流 ──────┘
        ↓
G6 双轮全量门禁与发布审计
```

禁止跳过依赖：没有 B1/B2 的任务恢复，不验收 C/D 任务重启；没有 D/E 的正式定稿契约，不验收 10 章连续；没有 B3/B4，不验收前端流式体验；没有 F5 数据往返证据，不宣称导入导出完成。

## 4. 功能分区、逻辑链、优化计划和验收标准

下列每个功能均使用统一逻辑链：

```text
用户操作 → 前端状态/参数 → API请求与schema → 权限/幂等 → TaskRuntime
→ worker租约/阶段事件 → Provider/业务服务 → 产物与账本
→ 持久化定稿 → SSE/轮询回读 → 前端展示 → 重启/取消/失败恢复
```

### A. 基础、安全和配置区

#### A1 登录、鉴权、用户隔离

- 现状范围：`auth.py`、dependencies、auth store、登录 API。
- 优化链：登录凭据 → token/session → 当前用户依赖 → 所有项目/任务/产物查询过滤 → 401/403结构化错误 → 前端恢复或重新登录。
- 优化计划：统一用户 DTO；审计所有路由是否强制 `current_user`；任务、文件、SSE、导出和日志逐项做 owner/project 双校验；禁止从客户端传入 user_id 作为权限依据。
- 验收：成功登录；错误凭据拒绝；跨用户项目/任务/事件/文件全部 404 或 403；过期 token 清晰提示；重启后会话行为一致；核心路由每项覆盖成功、拒绝、非法输入、异常恢复。

#### A2 项目和小说生命周期

- 现状范围：项目创建、读取、更新、删除、章节、阅读器、NovelWorkspace。
- 优化链：创建/编辑项目 → schema校验 → 项目元数据持久化 → 蓝图/账本初始化 → 章节和任务归属 → 前端缓存失效/刷新。
- 优化计划：统一项目 ID 类型和错误 DTO；明确软删除/不可删除条件；任务运行时禁止删除或给出可操作提示；项目复制、归档、恢复纳入审计。
- 验收：创建、编辑、刷新、空项目、已有章节项目、非法 ID、权限拒绝、运行任务时保护、重启后数据一致；项目下所有任务和产物不串项目。

#### A3 LLM、提示词、生成参数和 Token 预算

- 现状范围：`LLMSettings.vue`、`llm_config.py`、`llm_config_service.py`、`config_sync_manager.py`、Token服务。
- 优化链：前端修改 → schema校验/脱敏 → 配置版本递增 → 后端缓存失效 → 新任务读取配置快照 → 任务 metadata 记录模型/版本/参数 → 结果可追溯。
- 优化计划：配置快照不可被后续修改覆盖；模型 URL、密钥、模型名、候选数、温度、字数、分段、账本开关逐字段对齐；配置版本加入 TaskRuntime payload 和 ChapterVersion metadata；预算不足时提前阻止或可解释降级。
- 验收：前端改模型/字数/分段/候选/温度/提示词后下一任务全部读取新版本；旧任务保持旧快照；密钥不出日志；非法模型参数被拒；预算超限有明确错误；真实任务记录配置版本和模型为 `deepseek-v4-flash-free`。

#### A4 数据库、迁移、索引和租约

- 现状范围：session、init_db、Alembic、ledger lease、SQLite。
- 优化链：版本迁移 → schema基线 → 任务/事件/账本外键与唯一索引 → 事务边界 → 锁/重试 → 审计。
- 优化计划：所有迁移可重复执行；补齐任务幂等唯一约束、事件游标索引、项目/用户查询索引；明确 SQLite 开发限制与生产数据库方案；定稿使用事务或 outbox。
- 验收：fresh DB、旧 DB、重复迁移、并发写入、异常回滚、数据库重启、数据完整性检查；无静默 schema 漂移；锁冲突在预算内结构化收敛。

### B. 统一任务中心和实时区

#### B1 TaskRuntime 状态机

- 现状范围：`task_runtime.py`、模型、schema、所有任务 router。
- 目标状态：`queued → running → cancelling → cancelled/succeeded/failed/stale`；终态不可复活。
- 优化链：创建幂等任务 → claim租约 → heartbeat → 阶段事件 → 产物提交 → 终态事件；异常/重启 → stale → recover。
- 验收：每一合法转换成功；每一非法转换失败；取消中的迟到成功/失败事件不改变状态；终态追加事件不复活；worker 无心跳按阈值 stale；重启后可查询原 task_id。

#### B2 所有后台任务统一创建、去重、取消、重试和恢复

- 覆盖：正文、总纲、章节纲、蓝图、研究、文风、旧稿导入、导出、分析和图谱任务。
- 优化链：API idempotency key → TaskRuntime唯一约束 → 热缓存只存句柄 → worker领取 → 取消检查 → 终态收敛 → retry budget。
- 优化计划：逐个移除内存字典作为状态来源；所有 status/cancel/retry/recover 读库；建立统一 Task DTO；统一错误码和可执行动作；禁止重启重复入队。
- 验收：六类以上任务逐项清空内存缓存/模拟重启后状态不变；重复提交只产生一个任务；取消不复活；失败只按预算重试；stale 可恢复；无任务串项目。

#### B3 事件、运行日志和可观测性

- 优化链：worker阶段 → 结构化事件 → 持久化 event_id → 日志/正文类型分流 → UI回放/审计。
- 优化计划：统一事件 DTO（task_id、event_id、type、stage、progress、message、payload、artifact_ref、timestamp）；禁止把 Provider 原文密钥写日志；长文本预览截断但原文产物可追溯。
- 验收：每个任务至少有 queued/running/阶段/终态事件；事件游标单调；日志不含密钥；异常事件含 error_code、retryable、stage；重启后事件完整可回放。

#### B4 SSE、Last-Event-ID、心跳和终态关闭

- 优化链：鉴权 → 查 TaskRuntime → cursor 合并 → 回放持久化事件 → 实时订阅 → heartbeat → terminal close。
- 优化计划：正文 `content_delta` 和日志完全独立；统一 fetch-SSE 和路由行为；断线后不漏片、不重片；无活跃任务的终态流必须关闭；无效任务不可隐式创建。
- 验收：首次连接、断线、携带游标重连、从日志中间游标重连、最后正文片段重连、任务完成/失败/取消均通过；跨用户拒绝；每个正文片段可幂等拼接。

#### B5 并发和隔离

- 优化链：项目级 lease/idempotency → worker 并发控制 → Provider 限额 → 事件/正文按 task_id → 数据库校验。
- 优化计划：同项目同类任务去重；不同项目可并发；跨进程租约持久化；每个项目有独立上下文和事件流；记录排队时间、Provider耗时、数据库耗时。
- 验收：至少两个项目同时生成，任务 ID、正文、事件、版本、日志完全隔离；同项目重复提交只一任务；任何异常取消一个项目不影响另一个；并发下无 SQLite 未处理锁错误。

### C. 创作前工程区

#### C1 灵感和创作蓝图

- 逻辑链：灵感输入 → 结构化蓝图 → 角色/关系/世界观 → 项目绑定 → 蓝图版本 → 后续总纲/章节纲上下文。
- 优化计划：明确蓝图 schema；区分用户输入、AI建议、已确认内容；蓝图生成、修改、重试接入 TaskRuntime；支持局部编辑和版本回滚。
- 验收：创建、编辑、生成、取消、重试、重启查询、权限、版本回滚；蓝图被章节纲读取且任务 metadata 可追溯。

#### C2 研究资料、文献和内容收集

- 逻辑链：关键词/来源 → 抓取或上传 → 清洗分段 → 来源/时间/可信度 → 可引用片段 → 项目资料库 → 生成上下文。
- 优化计划：每条资料绑定 project_id、source、retrieved_at、credibility、citation_spans；失败可重试；取消不写半成品；引用进入 prompt 时保留来源引用 ID。
- 验收：成功、无效来源、超时、取消、重启恢复、权限隔离、重复资料去重；正文或大纲 metadata 可定位引用资料；禁止未标注资料静默进入正文。

#### C3 文风提取和风格库

- 逻辑链：上传/文本 → 文本安全检查 → 分段 → 风格画像 → 用户确认 → 项目/用户风格库 → 新任务读取版本。
- 优化计划：上传路径隔离；画像与原文分离；版本化风格；任务取消清理临时文件；风格不可覆盖正文内容和项目事实。
- 验收：TXT/DOCX输入、空文件、超大文件、非法路径、取消、重启、重复上传、跨用户隔离；生成任务记录使用的 style_profile_id/version。

#### C4 详细长篇总纲、卷纲和故事弧

- 逻辑链：题材/约束/研究 → 世界与角色基线 → 卷/篇章弧 → 主线/暗线 → 角色弧 → 伏笔表 → 章节依赖 → 用户确认。
- 优化计划：长篇按卷、弧、章、场景分层；每卷提供目标、冲突、转折、高潮、收束；每条伏笔有埋设/强化/回收窗口；禁止只生成空泛简介。
- 验收：短篇、中篇、长篇三档；长篇至少包含卷范围、章节范围、角色弧、伏笔表、依赖关系、节奏预算；章节纲可从总纲派生；重启/取消/失败可恢复。

#### C5 章节大纲和场景账本

- 逻辑链：总纲 + 前章账本 + 当前卷弧 → 章节使命 → 目标/冲突/信息增量 → 场景 goal/conflict/turn/outcome/hook → 下章约束。
- 优化计划：章节纲不得脱离当前连续性账本；字段必须结构化；生成和重写统一 TaskRuntime；用户编辑后标记版本并成为正文输入快照。
- 验收：生成、重写、编辑、确认、取消、重启、权限、重复提交；正文任务 metadata 含章节纲版本；正文至少能验证本章目标、冲突、转折、结尾钩子。

### D. 正文生成与质量区

#### D1 短章

- 逻辑链：章节纲/前文摘要 → 短章契约 → 单次 DeepSeek 正文 → 元文本检测 → 字数/质量门 → 事件流 → 版本/确认。
- 优化计划：单候选优先；无意义同源重试关闭；污染最多一次纯正文重试；低于最低字数才受控补足；失败结构化，不静默成功。
- 验收：真实 Provider 1200 字任务正文达到下限、首稿调用预算符合策略、正文 SSE可见、TaskRuntime终态正确、版本落库、正文无元文本；失败不无限等待。

#### D2 中长章、候选、评审和补足

- 逻辑链：章节任务 → 候选预算 → Provider候选 → guardrail → 自评/一致性 → 评审选择 → 质量门 → 补足或定稿。
- 优化计划：候选数由字数和质量风险解释性决定；禁止同源无意义候选；评审结果不得覆盖用户配置；失败候选保留诊断；最佳候选选择可复现。
- 验收：候选数和配置一致；评审失败有可操作终态；低质量候选不能静默定稿；质量结论和版本 ID 一致；重试不重复写入同一版本。

#### D3 2 万字以上分段生成

- 逻辑链：固定分段计划 → 段 1 Provider → checkpoint → content_delta → 段 2... → 拼接校验 → 全章质量门 → 原子定稿。
- 优化计划：段目标、最低字数、尾部上下文和 checkpoint 持久化；每段调用前取消检查；段失败只重试本段；恢复从最后确认段继续；禁止整章回退。
- 验收：真实 Provider ≥20,000 字；段顺序正确、总字数达标、每段事件可回放；中途取消进入 cancelling/cancelled；进程重启后从 checkpoint 恢复；故意重复/丢段时测试失败；最终定稿只产生一套章节账本。

#### D4 续写、改写、局部修订和补丁

- 逻辑链：选定版本/范围 → 上下文与约束快照 → Provider → diff/patch → 冲突检查 → 用户确认 → 新版本。
- 优化计划：原文版本 immutable；补丁使用 base_version_id；并发编辑冲突显式提示；局部修订不得改动未选范围；取消不覆盖正式版本。
- 验收：续写承接最后段；改写只影响范围；冲突被检测；撤销/重做；重启恢复；版本差异可读；最终选择明确。

#### D5 正文格式、元文本和安全守卫

- 优化链：Provider输出 → normalize → JSON/Markdown/任务说明检测 → 角色/禁写/POV守卫 → 质量门。
- 优化计划：正文、标题、日志、诊断分层；扩大中英文元文本检测但避免误杀正常叙事；污染正文永不进入正式版本；保留安全诊断。
- 验收：英文计划、中文提纲、JSON、Markdown、字数说明全部拒绝；正常小说不误报；污染重试不携带污染正文；长篇每段同样执行守卫。

#### D6 质量门、候选评审和原子定稿

- 逻辑链：正文候选 → 结构指标 → 自评 → 一致性 → 连续性 → 质量门 → 版本/摘要/快照/账本原子提交。
- 优化计划：将硬阻断和软警告分开；每个 blocker 给 code、message、evidence、retryable；定稿采用事务/outbox；终态和版本选择一致。
- 验收：低质量正文保留候选但不成功；每个 blocker 有证据片段；成功章节正文、摘要、快照、记忆、时间线、伏笔和版本同一提交批次可验证；事务中断可恢复，不出现半定稿。

### E. 连续性账本区

#### E1 项目记忆、摘要和章节快照

- 优化链：定稿 → 摘要 → global_summary → recent_snapshot → 下一章 context package。
- 优化计划：摘要和正文版本绑定；快照按章唯一；更新采用 outbox/幂等；上下文窗口有压缩策略但保留关键事实。
- 验收：1—10章每章摘要/快照存在；第11章可读全部历史摘要和最近快照；重试不会重复摘要；中断可恢复。

#### E2 人物、状态、关系和势力

- 优化链：角色基础档案 → 章节登场 → 状态变化 → 关系事件 → 势力归属 → 下一章可用上下文。
- 优化计划：区分角色事实、推断和临时状态；关系变化绑定事件和章节；禁止同名角色串档；正文后更新失败必须有可恢复 outbox。
- 验收：角色状态与正文事件一致；跨章节身份、年龄、伤势、关系不冲突；两个项目同名角色隔离；人工编辑不会被自动任务覆盖。

#### E3 伏笔、线索和回收

- 优化链：埋设 → 强化 → active → overdue → 回收/解释 → resolution。
- 优化计划：每条伏笔有唯一 ID、首次出现、承诺、预期窗口、回收证据；章节纲和正文都读写同一账本；超期提供可操作提示。
- 验收：新增、强化、回收、取消、删除保护、跨章追踪；回收状态和正文引用一致；未回收伏笔不被误报已回收。

#### E4 时间线、因果链和事件图

- 优化链：章节事件 → story time → event edge → causal chain → consistency gate。
- 优化计划：统一时间格式和时区；事件按项目/章节排序；冲突提供两侧证据；禁止模型自由改写已确认事实。
- 验收：时间顺序、因果方向、事件引用稳定；重复事件和冲突时间被检测；重启/导出/导入不丢事件。

#### E5 情绪曲线和节奏

- 优化链：章节目标/场景 → 情绪节点 → 曲线 → 章节评审 → 下一章节奏预算。
- 优化计划：曲线数据结构化；与正文段/场景绑定；只作为质量建议时不阻断，硬性缺陷才阻断；前端可读。
- 验收：生成、编辑、重算、导出、异常恢复；曲线与章节场景数量一致；不因缺少可选曲线阻塞正文。

### F. 业务工作台和数据区

#### F1 章节、阅读器、版本和评审

- 优化链：章节列表 → 版本选择 → 正文阅读 → 质量/评审 → 确认/定稿 → 导出。
- 优化计划：状态和版本选择来自 TaskRuntime/数据库；评审、补丁、版本差异均绑定 version_id；阅读器区分草稿/正式版。
- 验收：状态刷新一致；失败章节仍可查看候选和诊断；确认只选择一个版本；定稿后阅读器和导出读取同一正式版本。

#### F2 角色、势力、关系编辑

- 优化链：表单 → schema → 乐观锁/版本 → 数据库 → 账本上下文 → 生成任务。
- 优化计划：表单字段和后端模型逐项对齐；冲突编辑显式提示；删除有引用保护；变更写审计。
- 验收：增删改、非法输入、权限、并发编辑、回滚、生成读取最新版本；跨项目隔离。

#### F3 线索、伏笔、时间线、图谱和分析

- 优化计划：统一 overview 快照；避免重复 API 请求；所有图节点有项目归属和来源；空数据、错误、加载和刷新状态可理解。
- 验收：真实数据加载、空态、权限、异常、刷新一致；节点/线索编辑反映到后续章节上下文；不产生跨项目节点。

#### F4 导入导出 TXT/DOCX

- 优化链：导出选择 → 版本化结构包 → 文件生成 → artifact → 下载 → 上传 → schema解析 → 隔离项目恢复 → 完整性报告。
- 优化计划：TXT 和 DOCX 共享机读 manifest；明确支持/不支持字段；导出正文、蓝图、角色、关系、章节纲、版本、摘要、快照、记忆、线索、伏笔、时间线；导入逐项校验并报告缺失字段。
- 验收：导出→隔离库导入后逐项比对项目元数据、章节顺序、正文 hash、版本、蓝图、角色关系、章节纲、伏笔、线索、时间线、快照、记忆；TXT 和 DOCX 分别出覆盖报告；损坏文件、重复导入、权限和取消可恢复。

#### F5 管理台、日志、统计和限额

- 优化链：管理员权限 → 系统配置/更新日志/运行日志 → 统计与告警 → 审计。
- 优化计划：普通用户和管理员接口隔离；日志脱敏；统计使用持久化任务和用量；长任务有阶段耗时和 Provider 错误码。
- 验收：管理员成功、普通用户拒绝、非法输入、日志查询、分页、敏感信息脱敏、异常恢复；统计与数据库任务数一致。

### G. 前端体验和真实应用区

#### G1 左侧导航和信息架构

- 逻辑链：路由/当前项目 → 左侧功能入口 → 当前工作区 → 返回/刷新 → 深链可恢复。
- 优化计划：项目级导航与全局导航分层；移除重复顶部标签；每个按钮绑定真实事件；支持键盘焦点和窄屏折叠。
- 验收：所有功能入口可访问；当前项高亮；刷新/浏览器后退/深链一致；无死按钮；窄屏不遮挡正文。

#### G2 右上角进度卡、多任务切换

- 优化链：TaskRuntime snapshot → progress store → FloatingProgressCard → 取消/恢复/日志 → SSE/轮询更新。
- 优化计划：常驻顶部进度框彻底移除；卡片显示任务、阶段、百分比、已耗时、预计时间、可读等待说明、动作按钮；多个项目任务可切换且不串流。
- 验收：真实任务阶段来自持久化数据；卡片不遮正文；取消和恢复按钮状态正确；任务终态自动收起但可查看；多任务切换无状态串线。

#### G3 正文流、日志流和阅读器

- 优化链：SSE event → 类型映射 → 正文缓冲/日志缓冲 → 段幂等 → 刷新回放 → 正式正文。
- 优化计划：唯一 event mapper；禁止日志 payload 中的 delta 冒充正文；按 segment_index 去重；预览、草稿、正式版分区。
- 验收：正文只进入正文区，日志只进入日志区；断线重连无漏片/重片；刷新可恢复；长文本不阻塞主线程；错误能定位 task/stage。

#### G4 卡片、响应式、键盘和可访问性

- 优化计划：统一 tokens；减少嵌套卡片；内容密度分级；按钮主次明确；表单有 label/error/help；动画支持 reduced-motion；颜色对比和焦点可见。
- 验收：桌面、窄屏、键盘 Tab/Enter/Escape、屏幕阅读器语义、错误提示、加载态、空态、网络失败态；核心操作不依赖鼠标和颜色。

#### G5 浏览器完整工作流

- 验收流程：创建项目 → 灵感/蓝图 → 长篇总纲 → 章节纲 → 生成正文 → 查看正文流/日志 → 取消 → 恢复 → 评审 → 定稿 → 阅读 → 导出 → 隔离导入。
- 通过条件：每一步真实 API、真实状态、真实页面可见；刷新和断线至少各一次；任何失败都有可重试路径和用户可理解说明。

## 5. 分阶段实施计划

### 阶段 0：盘点和契约冻结

- 建立功能矩阵、路由矩阵、前后端字段矩阵、任务类型矩阵、数据产物矩阵。
- 为每个功能指定 owner、写入范围、测试文件、真实脚本和验收证据路径。
- 完成所有内存任务字典引用图；标记哪些是句柄、缓存、真相源。
- 产出：契约差异清单、P0 缺陷清单、依赖 DAG、基线门禁报告。

### 阶段 1：统一任务中心（P0）

- 先完成 B1 状态机不变量和 B2 统一 CRUD。
- 逐类迁移正文、总纲、章节纲、蓝图、研究、文风、导入、导出。
- 删除对外 status/cancel/retry 对内存字典的直接依赖；内存只保留 worker handle。
- 完成 B3/B4 事件和 SSE 全链路，再做 B5 双项目并发。

### 阶段 2：生成与连续性（P0）

- 完成 C4/C5 长篇规划和章节派生。
- 固化 D1 短章、D2 中长章、D3 20k 分段恢复、D4 修订、D5/D6 质量门与原子定稿。
- 完成 E1-E5 账本 outbox 和 10 章真实连续链。

### 阶段 3：业务数据完整性（P0/P1）

- 完成 A3 配置快照追溯。
- 完成 C2/C3/F2/F3 资料、文风、角色和图谱数据绑定。
- 完成 F4 TXT/DOCX 双向契约和隔离数据库比对。
- 完成 F5 管理、审计、用量和权限。

### 阶段 4：前端工作台（P1）

- 完成 G1 信息架构、G2 进度卡、G3 流分区。
- 再做 G4 视觉、响应式、可访问性和组件拆分。
- 最后执行 G5 浏览器完整流程，发现问题回到对应后端契约修复。

### 阶段 5：发布审计（贯穿）

- 每一阶段都执行定向测试、反向破坏、集成测试和真实入口。
- 全部 P0 通过后执行双轮全量门禁和并发/故障注入。
- 只有所有核心功能评分 ≥90，且无 P0，才允许标记 `RELEASED`。

## 6. 子代理并行分工和合并协议

| 代理 | 领域 | 只写入 | 不得修改 | 输出 |
|---|---|---|---|---|
| Agent A | TaskRuntime/SSE/并发/权限 | `PRODUCTION_PLAN_AGENT_TASK_RUNTIME.md` | 业务代码 | 状态机和任务中心计划 |
| Agent B | 生成/连续性/质量门 | `PRODUCTION_PLAN_AGENT_GENERATION.md` | 业务代码 | 生成链和长篇验收计划 |
| Agent C | 前端工作台/体验 | `PRODUCTION_PLAN_AGENT_FRONTEND.md` | 业务代码 | UI信息架构和浏览器验收 |
| Agent D | 研究/文风/蓝图/业务/导入导出 | `PRODUCTION_PLAN_AGENT_BUSINESS.md` | 业务代码 | 全业务功能计划 |
| Agent E | 测试/发布/性能/审计 | `PRODUCTION_PLAN_AGENT_RELEASE.md` | 业务代码 | 发布门禁和证据协议 |

合并规则：主线程读取五份结果 → 去重并核对文件证据 → 将差异补入本文档 → 再按不重叠写集委派实现任务。子代理不得同时修改同一模型、同一路由或同一组件；每个代码 worker 必须提交文件清单、测试结果和未解决风险。

## 7. 统一验收标准

### 7.1 每功能四类最低测试

1. 成功路径：合法输入产生正确业务产物。
2. 非法输入：schema、边界、空值、超限和格式错误得到结构化错误。
3. 权限隔离：未登录、普通用户、跨用户、跨项目均正确拒绝。
4. 异常恢复：Provider失败、超时、取消、断线、进程重启、数据库锁或重复提交可恢复或给出明确终态。

### 7.2 任务中心硬性不变量

- 同一幂等键只能有一个活动任务。
- 终态任务不可复活、不可重复入队。
- `cancelling` 不得被迟到 running/succeeded/failed 事件覆盖。
- 每个事件只属于一个 task_id；每个 task 只属于一个 owner/project/chapter。
- 事件游标严格递增；重连只返回大于游标的事件。
- 任务失败不得留下永久章节占用、永久 running 或不可解释的空白状态。

### 7.3 核心真实验收矩阵

| 编号 | 场景 | 必须有的证据 |
|---|---|---|
| R1 | DeepSeek 1200字短章 | 请求/配置版本、task终态、正文长度、版本、SSE正文片段、终态事件、耗时 |
| R2 | 1—10章连续 | 每章正文/摘要/快照/版本、下一章上下文、人物/伏笔/时间线账本、无串线 |
| R3 | 单章≥20,000字 | 分段计划、每段checkpoint、段序、段字数、正文delta、最终正文和定稿 |
| R4 | 20k取消与重启 | cancelling/cancelled或stale、最后checkpoint、恢复后不重写已确认段 |
| R5 | 双项目并发 | 两个 task/project ID、事件集合、正文 hash、版本和日志完全隔离 |
| R6 | 配置即时生效 | 前端保存版本、下一任务 payload、任务 metadata、模型/参数一致 |
| R7 | 导出导入 | TXT/DOCX文件、manifest、隔离库、字段差异报告、正文/版本/账本比对 |
| R8 | 全工作台 | 浏览器录屏/日志、每个页面截图或DOM断言、刷新/断线/取消/恢复证据 |

### 7.4 评分模型

每个功能 100 分，低于 90 不得进入下一阶段：

- 可用性 20：主路径和边界行为。
- 契约一致性 15：前端/API/schema/DB/任务 payload。
- 错误恢复 15：失败、取消、重启、重试、stale。
- 性能并发 15：耗时、资源、隔离和锁。
- 数据一致性 15：版本、账本、产物、事件。
- 自动化测试 10：正向、负向、反向破坏和集成。
- 体验与可访问性 10：进度、等待说明、布局、键盘和错误可理解性。

评分记录必须包含：commit/worktree 状态、命令、环境、task_id、project_id、数据库文件、事件数量、正文长度、Provider 错误码、耗时、失败重现步骤和剩余风险。

## 8. 固定门禁和执行批次

### 后端

```powershell
Set-Location backend
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m compileall -q app
Set-Location ..
```

### 前端

```powershell
Set-Location frontend
npm run type-check
npm run test:run
npm run build-only
Set-Location ..
```

### 真实入口

```powershell
backend\.venv\Scripts\python.exe backend\scripts\real_asgi_generation_smoke.py
backend\.venv\Scripts\python.exe backend\scripts\real_asgi_longform_generation_smoke.py
backend\.venv\Scripts\python.exe backend\scripts\real_asgi_concurrent_generation_smoke.py
```

真实入口默认使用隔离 SQLite、串行先行、并发单独批次；每次任务独立数据库和输出目录，禁止并行构建与全量测试抢占资源导致伪失败。

## 9. 发布阻断条件

任一条件成立就不得标记生产就绪：

- 存在永久卡死、静默失败、取消复活、跨项目泄漏或重启重复入队。
- 内存状态能覆盖 TaskRuntime 持久化终态。
- 真实短章、10章连续、20k分段、双项目并发、配置即时生效、导入导出任一缺证据。
- 任何核心功能评分低于 90。
- 全量后端、前端类型/测试/构建未连续两轮通过。
- 真实入口失败但只有“重试即可”文字，没有结构化诊断和可操作动作。
- DOCX 或附属账本字段的限制没有用户可见覆盖报告。

## 10. 持续执行循环

```text
选择当前依赖树上最高优先级功能
  → 读取代码和现有改动
  → 写缺陷复现测试
  → 最小实现修复
  → 正向 + 反向测试
  → 定向集成
  → 真实隔离入口
  → 按100分模型评分
  ├─ <90：记录缺陷，继续同功能迭代
  └─ ≥90：冻结契约，进入下一功能
  → 阶段门禁
  → 更新本计划和审计台账
```

当前下一执行点：先合并五个子代理的分区审查结果，锁定所有内存任务状态源和真实入口缺口；随后优先完成 B2/B4/B5，再进入 D3/E1 的真实长篇恢复验收。 

## 11. 已合并的交叉审查结论（2026-08-14）

以下结论来自领域 A、B、E 子代理对当前代码的逐文件审查，不代表这些项目已经修复：

### 11.1 P0 阻断清单

| 编号 | 结论 | 影响 | 第一执行动作 | 退出证据 |
|---|---|---|---|---|
| P0-RT-01 | research/style/blueprint/import/outline 仍存在 `_JOBS`、`_RUNS`、`_SCHEDULED_RUNS` 双轨 | 重启、跨实例、终态查询可能分叉 | 建立统一 `TaskView` 序列化器，逐路由让 status/cancel/retry/recover 只读 TaskRuntime | 清空内存字典后六类任务查询/恢复仍正确 |
| P0-RT-02 | 内存 `GenerationLogService` 与持久化日志服务并存 | 重启丢日志、调用方可能误用旧 API | 全量检索调用方，切换写入到 TaskRuntimeEvent，旧服务只保留兼容读适配 | 双进程/重启后日志游标完整 |
| P0-RT-03 | TaskRuntime 部分转移依赖 ORM 读改写，跨会话 CAS 证据不足 | 取消/完成/claim 竞态可能覆盖状态 | 增加 attempt、lease token、版本/CAS 更新和双 session 故障测试 | 双 worker 与取消/完成同时提交时只有一个合法终态 |
| P0-RT-04 | TaskRuntime SSE 与 updates SSE 事件视图不同 | 正文、日志、诊断可能在 UI 分区不一致 | 统一 event envelope 和 `channel=content/log/progress/diagnostic/terminal` | 游标重连无漏片、重片、跨频道污染 |
| P0-RT-05 | 20k 真实入口尚未覆盖段中断、进程重启、恢复 SSE 和旧 worker 迟到回调 | 长章可能重写已完成段或产生重复正文 | 增加段级故障注入和真实恢复脚本 | 断点恢复不重复调用已完成段，旧 lease 回调被拒 |
| P0-GEN-01 | 长篇大纲结构缺陷仍可能 warning 后保存，fallback 含“待填充” | 空泛大纲会污染后续长篇正文 | 将结构契约改为 fail-closed；fallback 只保存 degraded draft | 非法长纲不能成为正式生产大纲 |
| P0-GEN-02 | 大纲生成仍有同步 LLM 路由，未完全统一任务协议 | 长等待、取消、重启、幂等不一致 | 将总纲生成拆为 TaskRuntime 阶段任务 | 大纲任务支持状态、事件、取消、重启、重复提交 |
| P0-GEN-03 | 定稿缺少外部向量/账本 outbox，20k 摘要只看前 5000 字 | 正文与记忆/向量可能半提交或漏掉章末 | 引入 finalization session/outbox，摘要使用首/中/尾和结构事件 | 定稿中断可补偿，正文/摘要/账本同一 commit_id |
| P0-GEN-04 | “successful” 与 “waiting_for_confirm” 语义未统一 | 烟测可能把候选生成误判正式完成 | 冻结产品终态契约并同步 API、前端、脚本 | 生产章节正式状态只有一套可解释映射 |
| P0-REAL-01 | 真实 DeepSeek 仅有单章成功样本，曾出现 evaluation_failed | 无法证明稳定性和质量门无误报 | 串行基线 5/10 次，记录完整证据，不并发混跑 | 连续成功率、污染率、质量 blocker、耗时达标 |
| P0-REAL-02 | 真实 1—10 章连续、双项目并发、配置即时生效、DOCX 往返未完成 | 核心发布场景无证据 | 为每项建立独立 SQLite/真实入口批次 | 每项有 DB、事件、正文 hash、配置版本和脱敏输出 |

### 11.2 P1 结构和体验清单

- 统一 `TaskRuntime` DTO：动作、结构化错误、产物引用、attempt、事件频道、分片序号、内容 hash、诊断信息。
- 把 ChapterMission 版本化，场景目标必须绑定正文证据片段、状态变化和因果后果，不能只用关键词命中。
- 将事实账本与 LLM 摘要分层；死亡、时间、位置、物品、秘密和伏笔状态等硬事实不能被摘要覆盖。
- 伏笔回收验证“揭示 + 因果 + 后果”，关键词命中只能作为提示。
- 统一终态的可执行动作：查看候选、查看诊断、重试、恢复、确认、导出。
- 真实浏览器 E2E 补齐键盘、窄屏、SSE 断线、多任务切换和错误恢复。
- 核对 `verify.ps1` 旧 E2E 与 `real_asgi_*.py` 新入口覆盖差异，发布门禁不能只跑旧脚本。
- SQLite WAL、迁移锁、双进程迁移、长 SSE 连接和多实例 sweeper 需要独立实测。

## 12. 当前执行队列（可直接转成代码工单）

```text
Q0 证据冻结
├─ Q0.1 记录 git/worktree、环境版本、当前门禁和真实短章基线
├─ Q0.2 生成所有任务入口的 status/cancel/retry/recover 调用图
└─ Q0.3 冻结 TaskRuntime/事件/终态/配置快照 DTO
      ↓
Q1 任务中心收口
├─ Q1.1 内存 job 仅保存 worker handle，状态全读 DB
├─ Q1.2 合并双日志服务和双 SSE 事件视图
├─ Q1.3 增加 attempt/lease/CAS/outbox 竞态保护
└─ Q1.4 真实双项目、取消、断线、重启批次
      ↓
Q2 生成契约收口
├─ Q2.1 长篇总纲 fail-closed schema
├─ Q2.2 ChapterMission version/hash/evidence
├─ Q2.3 短章终态和真实串行基线
├─ Q2.4 20k 段中断/恢复/旧 worker 故障注入
└─ Q2.5 finalization session + outbox + 首中尾摘要
      ↓
Q3 账本和业务完整性
├─ Q3.1 事实账本/摘要分层
├─ Q3.2 角色/势力/伏笔/线索/时间线/图谱一致性投影
├─ Q3.3 配置版本真实回读
└─ Q3.4 TXT/DOCX manifest 往返比对
      ↓
Q4 前端真实工作流
├─ Q4.1 左侧导航全入口接线
├─ Q4.2 右上角进度卡多任务/取消/恢复
├─ Q4.3 正文/日志/诊断三流分区
└─ Q4.4 浏览器完整流程和可访问性
      ↓
Q5 发布准入
├─ Q5.1 故障注入矩阵和反向测试
├─ Q5.2 后端/前端/迁移分批门禁
├─ Q5.3 真实短章/10章/20k/并发/配置/往返
└─ Q5.4 连续两轮全量 + 每功能评分 ≥90
```

## 13. 当前状态声明

- 总计划：`IMPLEMENTED_UNVERIFIED`（计划已写入并分派审查，不代表功能已修复）。
- DeepSeek 短章：已有一次 `REAL_VERIFIED` 成功样本，但稳定性目标仍为 `PENDING`。
- TaskRuntime：核心服务为 `TESTED`，全业务唯一真相源为 `PENDING`。
- 长篇分段：服务契约为 `TESTED`，真实 20k 中断/重启恢复为 `PENDING`。
- 10 章连续、双项目并发、配置即时生效、DOCX 往返、完整浏览器流程：`PENDING`。
- 发布准入：`BLOCKED`，直到所有 P0 证据齐全且双轮门禁通过。
